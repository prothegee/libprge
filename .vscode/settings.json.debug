{
    "cmake.generator": "Ninja",
    "cmake.buildDirectory": "${workspaceFolder}/.build/${buildType}",
    "cmake.configureSettings": {
        // default is false
        "LIBPRGE_EXAMPLE": false,

        "_": "don't use me"
    }
}
